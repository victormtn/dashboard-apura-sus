services:
  - type: web
    name: dashboard-apura-sus
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/dashboard.py
    env: python
    region: oregon  # ou outro como 'frankfurt', 'singapore', etc.
    plan: free
    autoDeploy: true